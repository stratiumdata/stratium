# Copy to .env and customize for local Docker runs.

# Go FIPS module build setting used by docker-compose build args.
# Keep this aligned with security.fips.go_module in service config.
GOFIPS140=v1.0.0-c2097c7c
