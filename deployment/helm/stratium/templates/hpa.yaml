{{- if and .Values.platform.enabled .Values.platform.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-platform
  labels:
    {{- include "stratium.platform.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-platform
  minReplicas: {{ .Values.platform.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.platform.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.platform.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.platform.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.platform.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.platform.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
---
{{- if and .Values.keyManager.enabled .Values.keyManager.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-key-manager
  labels:
    {{- include "stratium.keyManager.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-key-manager
  minReplicas: {{ .Values.keyManager.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.keyManager.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.keyManager.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.keyManager.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.keyManager.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.keyManager.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
---
{{- if and .Values.keyAccess.enabled .Values.keyAccess.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-key-access
  labels:
    {{- include "stratium.keyAccess.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-key-access
  minReplicas: {{ .Values.keyAccess.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.keyAccess.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.keyAccess.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.keyAccess.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.keyAccess.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.keyAccess.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
---
{{- if and .Values.pap.enabled .Values.pap.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-pap
  labels:
    {{- include "stratium.pap.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-pap
  minReplicas: {{ .Values.pap.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.pap.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.pap.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.pap.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.pap.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.pap.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
---
{{- if and .Values.papUI.enabled .Values.papUI.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-pap-ui
  labels:
    {{- include "stratium.papUI.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-pap-ui
  minReplicas: {{ .Values.papUI.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.papUI.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.papUI.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.papUI.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
{{- end }}
---
{{- if and .Values.envoy.enabled .Values.envoy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stratium.fullname" . }}-envoy
  labels:
    {{- include "stratium.envoy.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stratium.fullname" . }}-envoy
  minReplicas: {{ .Values.envoy.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.envoy.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.envoy.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.envoy.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
{{- end }}